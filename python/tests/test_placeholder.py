"""Placeholder so `python -m pytest python/tests` has something to collect.

Real coverage (bindings round-trip, simulator determinism, strategy
accounting) is added starting Milestone 6.
"""


def test_placeholder() -> None:
    assert 1 + 1 == 2
